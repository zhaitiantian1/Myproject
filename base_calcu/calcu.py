class Cacul():
    def add(self, a, b):
        return a + b

    def sub(self, a: float, b: float) -> float:
        return a - b

    def div(self, a: tuple) -> float:
        return a[0] / a[1]

    def muti(self, a: tuple) -> float:
        return a[0] * a[1]
